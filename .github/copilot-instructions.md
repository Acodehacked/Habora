# Habora workspace instructions

- Use TypeScript and the Next.js App Router.
- Keep Supabase access behind helpers in `src/lib/supabase`.
- Keep secrets in environment variables; never commit `.env.local`.
- Prefer small, accessible components and preserve the calm, warm Habora visual language.
- Run `npm run lint` and `npm run build` before handing off changes.
